function wf = extrapolate_field(SIM, MESH, wf)
% This routine extrapolates field values into the ghost layer
% NOTE; this type of functino makes uses pass by value, so a temp copy is
% made in memory



end % function

